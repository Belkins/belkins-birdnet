# Dev defaults to mock so `npm run dev` works on a Mac with NO backend.
# Override with `.env.development.local` (or inline `VITE_MOCK=0 npm run dev`)
# to point dev at a real birdcast. Production builds do NOT read this file.
VITE_MOCK=1
